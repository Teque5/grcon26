# DTMF Challenge

## Solution

* Topic: DSP / Voice Grade Channel / Phreaking
* `flag{ITU-TRecQ.23}`

## Explanation

This is a puzzle where one is meant to recognize the dual tone multi frequency encoding of a sequence of numbers representing a message string. The message is encoded as a sequence of numbers representing ascii characters

## Generating the wav file

run `python3 generate_wav.py`

### Dependencies

* numpy
* scipy
